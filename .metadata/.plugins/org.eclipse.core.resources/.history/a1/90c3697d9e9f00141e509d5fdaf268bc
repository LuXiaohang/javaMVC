package robot;//1行
import java.net.URL;
/*
 * 本类定义了队列的元素 
 */ 	//5行
public class Link  {
	private URL pageUrl; //待爬行的URL 
    private int depth;// URL的层数
    //返回要搜索的网页URL  //10行
    public URL getURL (){
    	return pageUrl;
    }  
    //返回要搜索的网页层数
    public int getDepth (){  //15行
        return depth; 
    }
    //设置要搜索的网页URL
    public void setURL(URL url) {   
        this.pageUrl = url; //20行
    } 
    //设置要搜索的网页层数
    public void setDepth (int depth) {
        this.depth = depth;
    }//25行
}